# Intentionally contains no Harness Binder keep rules.
#
# This application is the packaged external-consumer fixture. Any optimizer
# requirements owned by the public Consumer SDK must arrive transitively from
# the published/library AARs rather than being patched into the consumer app.
