name: LLUP exact-source package evidence dispatch

on:
  push:
    branches:
      - ci/llup-package-evidence
    paths:
      - .github/workflows/llup-package-evidence-dispatch.yml

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    name: Dispatch final exact-source LLUP candidate package
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Dispatch frozen LLUP v0.3.0 runtime candidate package
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          TARGET_SHA: f796235f47899314b65a5abc95c998b396883c4b
        shell: bash
        run: |
          set -euo pipefail
          payload="$(printf '{\"ref\":\"dev\",\"inputs\":{\"candidate_ref\":\"%s\",\"profile\":\"false\"}}' "$TARGET_SHA")"
          curl --fail-with-body --silent --show-error \
            --request POST \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer $GH_TOKEN" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/$REPOSITORY/actions/workflows/package.yml/dispatches" \
            --data "$payload"
